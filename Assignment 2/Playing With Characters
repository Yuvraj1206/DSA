#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
char c;
char s[100];
char p[100] ,k[900];
scanf("%c",&c);
gets(k);
gets(s); 
gets(p);
printf("%c",c);
printf("\n%s\n%s",s,p); 

    return 0;
}
